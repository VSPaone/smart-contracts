# Application Configuration
PORT=4000
API_BASE_URL=http://localhost:4000/api

# MongoDB Configuration (Testing Database Instance)
MONGO_URI=mongodb://localhost:27017/smartcontracts-test

# Security Configuration
JWT_SECRET=test-jwt-secret-key
ENCRYPTION_KEY=test-encryption-key
JWT_EXPIRATION=1h

# Node Configuration
MAX_NODE_CONNECTIONS=10
STATE_SYNC_INTERVAL=60  # Reduced interval for faster testing

# Logging Configuration
LOG_DIRECTORY=./logs/test
LOG_LEVEL=debug

# Rate Limiting Configuration
RATE_LIMIT_WINDOW=600000  # 10 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=50  # Lower limit for controlled testing

# Miscellaneous Configuration
CACHE_TTL=600  # Cache Time-to-Live in seconds (10 minutes)
DEFAULT_LANGUAGE=en
